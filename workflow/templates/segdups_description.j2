This contains all segmental duplications:

`{{ segdups_file }}`

This contains all segmental duplications >= 10,000 base pairs:

`{{ long_segdups_file }}`

These are the complements of the above:

`{{ not_segdups_file }}`
`{{ not_long_segdups_file }}`
